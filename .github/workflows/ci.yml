name: CI

on:
  push:
    branches: [ "main", "1.*", "2.*" ]
  pull_request:
    branches: [ "main", "1.*", "2.*" ]

jobs:
  test-and-analyze:
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        php: ['8.4']

    services:
      redis:
        image: redis:alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, json, sockets, pcntl, posix, swoole, redis
          coverage: none

      - name: Validate composer.json
        run: composer validate --strict

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Run PHPStan (Static Analysis)
        run: composer analyze

      - name: Run PHPUnit (Tests)
        run: composer test
